spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        namespace: dev
        ext-config:
          - data-id: snail-global.yml
            refresh: true
            group: dev
        password: nacos
        username: nacos
  application:
    name: snail-sys
